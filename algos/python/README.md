# Python Algorithms

This folder is reserved for future Python policy agents, RL training code, and
model-backed agents.

For now, production game integration goes through the Python `BaseAgent`
interface or the external JSONL process contract documented in
`docs/002_agent_contracts.md`.
